# Requirements for European Power Grid Builder v2.0 Enhanced
# Advanced power system simulation with U/Q Control and Blackstart Capability

# Core dependencies
numpy>=1.21.0
flask>=2.0.0
flask-socketio>=5.0.0
python-socketio>=5.0.0
websockets>=10.0.0

# Enhanced numerical computing for U/Q Control and Blackstart
scipy>=1.7.0
matplotlib>=3.5.0
seaborn>=0.11.0

# European power system modeling
pandas>=1.3.0
networkx>=2.6.0

# Advanced optimization for restoration sequences
cvxpy>=1.2.0

# Optional Fortran acceleration (requires gfortran compiler)
# f2py is included with NumPy >= 1.21.0
# Install gfortran: sudo apt-get install gfortran (Ubuntu/Debian)
# Install gfortran: brew install gcc (macOS)
# Install gfortran: sudo yum install gcc-gfortran (RHEL/CentOS)

# Development and testing (optional)
pytest>=6.0.0
pytest-cov>=2.12.0
